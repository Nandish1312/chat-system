Instruction how to run the code:

1) clone the repository by "git clone https://github.com/Nandish1312/chat-system"
2) open up the terminal and make your way through the cloned repository
3) run "npm install" command
4) run "node index.js" command
5) open up any browser and enter url, "http://localhost:3000"
6) Voila!